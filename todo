global tests:
    - extend filters test to ListFields and EmbeddedDocumentFields
    - test if unique constaint work and give right response
    - invalid put request on base resource without an id in the url

subdocument tests:
    - test filters
