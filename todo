- tests for subdocuments:
    - post (for adding items to listfields)
    - create on PUT

global tests:
    - filters (e.g. ?name=bla)
    - test if unique constaint gives right response
    - invalid put request on base resource without an id in the url
